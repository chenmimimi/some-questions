var startX = 0;
var translateX = 0;
var isMoving = false;
var isActive = false;
var optionWidth = 0;

function touchStart(e, ownerInstance) {
  var instance = ownerInstance.selectComponent('.swipe-action-options');
  optionWidth = instance.getDataset().width;
  startX = e.touches[0].clientX;
  isMoving = true;

}

function touchMove(e, ownerInstance) {
  var instance = ownerInstance.selectComponent('.swipe-action-ctn');
  var moveX = 0;
  if(isMoving) {
    moveX = e.touches[0].clientX - startX + (isActive? -optionWidth: 0);
    if(moveX < 0 && moveX > -optionWidth) {
      translateX = moveX;
    }else if(moveX <= -optionWidth){
      translateX = -optionWidth;
    }else{
      translateX = 0;
    }
    instance.setStyle({
      'transform': 'translate3d(' + translateX + 'px, 0px, 0px)'
    })
  }
  return false;
}

function touchEnd(e, ownerInstance) {
  var instance = ownerInstance.selectComponent('.swipe-action-ctn');
  isMoving = false;
  if(translateX > -optionWidth/2){
    closeOption(instance);
  }else{
    translateX = -optionWidth;
    instance.setStyle({
      'transform': 'translate3d(-' + optionWidth + 'px, 0px, 0px)',
      'transition': '.3s all ease-in-out',
    });
    isActive = true;
    return false;
  }
}

function onTap(e, ownerInstance) {
  var instance = ownerInstance.selectComponent('.swipe-action-ctn');
  if(isActive){
    closeOption(instance)
  }else{
    ownerInstance.triggerEvent('onTap', {}, {});
  }
}

//关闭操作项
function closeOption(instance) {
  translateX = 0;
  instance.setStyle({
    'transform': 'translate3d(0px, 0px, 0px)',
    'transition': '.3s all ease-in-out',
  });
  isActive = false;
}

module.exports = {
  touchStart: touchStart,
  touchMove: touchMove,
  touchEnd: touchEnd,
  onTap: onTap,
}
